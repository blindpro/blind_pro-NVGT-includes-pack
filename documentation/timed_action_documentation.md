# timed action

This is a class used for cichuations where you want a intager and a timer to interact.

Call the constructer to set its fields, then use the trigger or check methods to return booleans on if the timer has elapsed the interval given.